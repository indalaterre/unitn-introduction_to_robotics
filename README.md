#### 1. ROS/Locosim Visualization
```bash
# Launch the following script from the main project folder
# This contains all the commands to successfully Launch RViz
./launch_cleaner.sh
```

```bash
# Terminal 2: Run the simulation
cd src/cleaning_robot/scripts
python3 run_sim.py --mode ros --duration 20
```
